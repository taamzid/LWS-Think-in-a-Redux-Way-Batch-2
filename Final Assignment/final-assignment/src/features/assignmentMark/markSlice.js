import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const markSlice = createSlice({
  name: "assignmentMark",
  initialState,
  reducers: {},
});

export const {} = markSlice.actions;
export default markSlice.reducer;
